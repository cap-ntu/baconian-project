language: python
python:
  - "3.5"
dist: xenial

before_install:
  -

install:
  - bash installation.sh

script:
  - pip install pytest pytest-cov
  - pytest --cov=baconian ./examples/ ./baconian/test/
branches:
  only:
  - master
  - travis_ci_test
  - dev_*
notifications:
  email: false

after_success:
  - codecov